{% extends 'admin/base.admin.html.twig' %}
{% block body %}
    <div class="container col-full">
        <h1 class="text-center">Entitées de {{ entities.name }}</h1>
        <table class="table_admin">
            <thead>
                <tr>
                    {% for field in entities.fields %}
                        <th>{{ field }}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {% for data in entities.data %}
                    <tr>
                        {% for field in entities.fields %}
                            <td>{{ data[field] }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}